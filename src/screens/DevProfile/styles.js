import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  subContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  devPic: {
    height: 120,
    width: 120,
  },
  devNameText: {
    fontSize: 18,
    color: 'black',
    fontWeight: '600',
    padding: 20,
    textAlign: 'center',
  },
});
